# Cargo configuration for the Events Manager workspace

# Enable getrandom's JS backend for wasm32-unknown-unknown
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
